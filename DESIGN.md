# KuiklyAIStock 设计规范

## 设计目标

面向高频看行情场景，优先保证数据扫描效率、行情状态可信度与跨端一致性。界面用克制的白色表面 + 细分隔线，不做大面积装饰性卡片。

## 颜色语义

| 语义 | 色值 |
| --- | --- |
| 品牌/交互 | `#246BFD` |
| 上涨 | `#E64545` |
| 下跌 | `#1A9B62` |
| 平盘/辅助文字 | `#667085` |
| 风险 | `#B54708` |
| 页面背景 | `#F6F7F9` |
| 主文字 | `#101828` |
| 分隔线 | `#EAECF0` |

行情涨跌必须同时显示正负号，不能只依赖颜色。色值统一走 `StockDesignTokens`。

## 排版与间距

- 页面水平边距 16dp；行情列表行高 68dp；触控目标 ≥ 44dp。
- 标题 20-22sp；列表名称 16sp；代码/辅助说明 11-12sp；详情价格 32sp。
- 价格与涨跌数值右对齐，避免扫描时跳动。
- 只有市场摘要、图表、AI 汇总用 8dp 圆角容器；普通列表用 1dp 分隔线。
- 底部导航顶部加 1dp 分隔线。

## 页面结构

### 行情首页（单页三 Tab）

- 顶部栏：白色表面、居中标题（行情/自选显示「AI 股票」，AI 显示「AI 解读」）。
- 行情/自选 Tab：市场概览卡片（指数、涨跌家数、样本成交额）+ 高密度行情列表（名称/代码 + 最新价/涨跌幅 + 星标）。
- AI Tab：市场观点卡片 + 重点股票解读卡片。
- 底部栏：行情 ⌁ / 自选 ♡ / AI解读 ✦ 三入口，品牌蓝选中态；自选星标用 ♡/★ 区分。

### 股票详情

按「价格 → 关键行情 → 走势 → AI 观点」排列：

- 身份区：股票名 + 代码 + 星标。
- 价格面板：最新价 32sp + 涨跌额/涨跌幅。
- 关键行情网格：今开/昨收/最高/最低/成交量/成交额。
- 走势：分时/日K 切换 + Canvas 折线，标注昨收基准线、最高、最低、当前。
- AI 观点：事实摘要、观点判断、关注要点、信号解读、依据、风险提示，含「演示数据」标识。

### 状态反馈

加载态（骨架）、空态、错误态 + 重试。条件/循环渲染统一用 `vif`/`velse` 与响应式 getter，不在 `body()` 里用普通 `if`/`when`/`forEach`。

## 实现状态（2026-09-08）

- 已完成：单页根 Tab、股票专用导航栏、市场摘要、高密度列表、分时/日K、昨收/高/低标识、AI 分层、异步 Repository、响应式渲染。
- 待验收：Android 真机 360x800、长名称、长数值、空/失败状态与返回栈。
- 明确延期：Task 2、搜索、提醒、周K/月K、缩放、十字光标、全屏、深色模式、真实行情与 AI 服务。
