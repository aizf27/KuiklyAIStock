# KuiklyAIStock 设计规范

## 设计原则

- 面向高频行情浏览，优先保证数据可扫读、状态明确和跨端一致。
- 页面保持克制：浅灰背景、白色主卡、少量品牌蓝强调。
- 涨跌必须同时展示正负号和颜色，不能只依赖颜色传达。
- 视觉常量统一维护在 `StockDesignTokens`，页面内不重复定义。

## 视觉 Token

| 语义 | 规范 |
| --- | --- |
| 页面背景 | `#F6F7F9` |
| 卡片表面 | `#FFFFFF` |
| 主文字 | `#101828` |
| 次要文字 | `#667085` |
| 品牌色 | `#246BFD` |
| 上涨 / 下跌 | `#E64545` / `#1A9B62` |
| 控件背景 | `#F0F2F5` |
| 资讯卡背景 | `#E9EDF3` |
| 资讯卡层次色 | `#D7DEE8` |
| 分隔线 | `#EAECF0` |

- 页面水平边距、卡片内边距：`16dp`。
- 卡片圆角：`12dp`。
- 普通模块间距：`16dp`；页面大区块间距：`24dp`。
- 最小触控区域：`44dp × 44dp`。
- 价格和涨跌幅右对齐，长名称与长数值不得撑出内容轨道。

## 页面骨架

- 顶部栏必须消费状态栏、刘海和安全区，标题位于安全区下方。
- 底部导航保留 `64dp` 内容高度，并增加系统手势区占位。
- 滚动内容底部预留导航高度、安全区和 `24dp` 余量。
- 行情、自选、详情页统一使用“屏幕宽度 - 左右各 16dp”的内容宽度。

## 行情首页

固定区域：

1. 标题栏。
2. 搜索框：支持按股票名称或代码过滤 Mock 排行。
3. `大盘 / 板块 / 个股` 分类栏；默认大盘，其他分类暂为空态。

滚动区域：

1. 市场状态核心卡：A 股状态、样本资金、三大指数、涨跌对比、成交额和数据时间。
2. 时效资讯卡：盘前必读、午盘点评、收盘点评或晚间复盘。
3. Mock 股票排行：名称、代码、最新价、涨跌幅和收藏入口。

不展示 `A股 / 港股 / 美股` 市场切换。交易状态按系统时间计算；交易时段展示当前时间，非交易时段展示 Mock 样本时间。

## 自选与详情

- 自选页复用行情列表行、收藏状态和统一页面骨架。
- 详情页按“价格 → 关键行情 → 走势 → AI 观点”排列。
- 走势图卡片、Canvas 和内部绘图区必须共用同一可用宽度，分时与日 K 不得横向裁切。
- AI 内容明确标注演示数据、依据和风险提示。

## 状态与进度

- 加载、空、错误和重试状态使用响应式 getter 与 `vif` / `velse` 渲染。
- 已完成：统一安全区、内容轨道、行情首页可用版、搜索、分类、核心卡、资讯卡、排行和详情走势图适配。
- 待验收：Android 真机视觉微调、长名称/长数值及多屏宽验证。
- 延期：真实行情与 AI 接口、Task 2 完整问答、深色模式和高级图表交互。

更新日期：2026-09-10。
