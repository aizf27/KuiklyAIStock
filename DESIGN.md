# KuiklyAIStock 设计规范

## 设计目标

面向高频看行情场景，界面优先保证数据扫描效率、行情状态可信度和跨端一致性。页面使用克制的白色表面与细分隔线，不采用大面积装饰性卡片。

## 颜色语义

- 品牌/交互：`#246BFD`
- 上涨：`#E64545`
- 下跌：`#1A9B62`
- 平盘与辅助文字：`#667085`
- 风险：`#B54708`
- 页面背景：`#F6F7F9`
- 主文字：`#101828`

行情涨跌必须同时显示正负号，不能只依赖颜色。

## 排版与间距

- 页面水平边距：16dp。
- 行情列表行高：68dp；星标等触控目标不小于 44dp。
- 标题使用 20-22sp，列表名称 16sp，代码和辅助说明 11-12sp。
- 价格和涨跌数值必须右对齐，避免列表扫描时跳动。
- 只有市场摘要、图表和 AI 汇总使用 8dp 圆角容器；普通列表使用分隔线。

## 页面规则

- 首页首屏展示市场状态、数据时间、两个指数、涨跌家数、样本成交额和至少三条行情。
- 详情页按“价格 -> 关键行情 -> 图表 -> AI 观点”排列；价格和关键行情使用白色轻量分组与 1dp 分隔线。
- 走势图必须标注昨收、最高、最低；`TrendPoint.label` 仅作为横轴展示标签，不承担时间计算语义。
- AI 内容必须区分事实摘要、观点参考、信号解读、适用周期、依据与风险，并显示演示数据标识。
- 未实现的搜索、提醒、聊天、资讯入口不显示，不能放置无响应控件。

## 实现状态（2026-09-08）

- 已完成：单页根 Tab、股票专用导航栏、市场摘要、高密度列表、分时/日 K、昨收/高/低标识、AI 分层、异步状态与模板隔离。
- 待验收：Android 真机 360x800、长名称、长数值、空/失败状态和返回栈。
- 明确延期：Task 2、搜索、提醒、高级图表交互、深色模式和真实服务。
